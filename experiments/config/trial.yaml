defaults:
  - _self_
  - datasets: default

model:
  _target_: py_boost.gpu.sketch_boost.SketchBoost
  _partial_: true  # This tells Hydra to create a partial

experiment:
  run_name: "trial"
  skip_datasets: 
    - rt_iot2022
    - genbase
    - birds
    - yeast
    # - age_prediction
  skip_first: {}

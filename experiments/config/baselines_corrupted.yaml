defaults:
  - _self_
  - datasets: default

model:
  _target_: py_boost.gpu.sketch_boost.SketchBoost
  _partial_: true  # This tells Hydra to create a partial

experiment:
  _target_: experiments.runners.corrupted_labels.CorruptedLabelsBaselinesExperiment
  run_name: "baselines_corrupted"
  skip_datasets: 
    # - yeast 
    # - rt_iot2022
    # - genbase
    # - birds
    - mediamill
    - age_prediction
  skip_first: {}

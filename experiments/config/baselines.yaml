defaults:
  - _self_
  - datasets: default

model:
  _target_: py_boost.gpu.sketch_boost.SketchBoost
  _partial_: true  # This tells Hydra to create a partial

experiment:
  run_name: "baselines_4.0"
  skip_datasets:
    - rt_iot2022
    - mediamill
    - mbd
    - age_prediction
  skip_first: 
    # age_prediction: 191
    # yeast: 191
